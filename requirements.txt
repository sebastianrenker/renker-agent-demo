# Core demo (gateway, scripted run, tests) needs only renker-core-authz.
# The MCP server additionally needs `mcp` — install with:  pip install -e ".[mcp]"
renker-core-authz @ git+https://github.com/sebastianrenker/renker-core-authz.git
